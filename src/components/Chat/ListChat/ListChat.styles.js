import { StyleSheet } from "react-native";

export const styles = new StyleSheet.create({
  content: {
    paddingBottom: 100,
  },
  noChats: {
    textAlign: "center",
    marginTop: 20,
    color: "#fff",
    opacity: 0.6,
    fontSize: 16,
    marginHorizontal: 40,
  },
  text: {
    fontSize: 16,
    color: "#333",
  },
});
